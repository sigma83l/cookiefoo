const { i18n } = require('i18next');

module.exports = {
  i18n,
  defaultNS: 'common',
  defaultLanguage: 'fa',
  otherLanguages: ['en', 'de'], // Add more languages as needed
  localePath: 'public/translate',
};
